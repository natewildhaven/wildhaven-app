I need you to refactor this exported monorepo so the backend deploys cleanly on Vercel as a serverless API, without breaking the frontend.

Context:

* Frontend is already deployed on Vercel from `artifacts/wildhaven`
* Backend code currently lives in `artifacts/api-server`
* Repo root now has `api/index.js` intended for Vercel
* Vercel backend project is using repo root as root directory, framework preset "Other", build command `echo skip-build`, and no output directory
* The current backend deploy is failing because the TypeScript/Express backend is not Vercel-compatible in its current form

What I need you to do:

1. Refactor the backend so it works on Vercel serverless functions
2. Minimize manual changes from me
3. Keep the API endpoints and behaviour intact as much as possible
4. Make the repo structure and imports Vercel-compatible
5. Tell me exactly what changed and what Vercel settings I should use after your refactor

Please make these changes:

A. Vercel entrypoint

* Create or update a root-level Vercel API entrypoint so Vercel can serve the backend from the repo root
* Prefer a root file like `api/index.ts` or `api/index.js` that correctly exports the Express app for Vercel
* Make sure this entrypoint works with Vercel’s API routing

B. Express app structure

* Ensure the Express app is exported from a reusable module and is not calling `app.listen(...)` in the Vercel path
* If there is still a standalone server entry for local dev, separate it cleanly from the Vercel handler
* Keep local development working if possible

C. TypeScript / ESM compatibility

* Fix all relative imports in backend TypeScript files to be compatible with NodeNext/ESM where required
* Add `.js` extensions to relative imports where needed
* Fix typing issues caused by explicit `IRouter` / `Express` typings if they break the Vercel/TS build
* Ensure route files compile cleanly under Vercel’s TypeScript environment

D. Monorepo/workspace compatibility

* Fix any broken imports from workspace packages used by the backend
* If workspace package imports like `@workspace/db` or `@workspace/api-zod` are not Vercel-compatible in this backend deployment, either:

  * make them compile correctly in this deployment setup, or
  * replace only the minimum required pieces in the backend with local imports or local types so the backend can build
* Do not break the rest of the monorepo

E. Route compatibility

* Update all backend route files so they build on Vercel
* Fix Express router typings and request/response typing issues cleanly, without reducing the app to only a health endpoint
* Keep the real routes working

F. Build/deploy compatibility

* Make the backend deployable on Vercel without relying on Replit-specific environment assumptions
* Remove or isolate Replit-specific runtime requirements from the backend deploy path
* If a vercel.json is needed, create the correct one
* If a different Vercel setup is better, explain it clearly

G. Output
After making changes, give me:

1. a summary of every file you changed
2. the exact Vercel settings for the backend project:

   * root directory
   * framework preset
   * build command
   * output directory
   * install command
3. any environment variables I need to add in Vercel
4. the URL paths I should test first, such as `/api/healthz`

Important:

* Do not just patch one route at a time
* Do a full Vercel backend compatibility pass across the backend
* Preserve as much existing functionality as possible
* Explain any compromises you had to make
